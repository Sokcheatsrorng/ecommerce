/** @type {import('next').NextConfig} */
const nextConfig = {
    images :{
        domains: [
            'store.istad.co', 'hips.hearstapps.com', 'i.pinimg.com','fakestoreapi.com'
        ]
    }
};

export default nextConfig;
